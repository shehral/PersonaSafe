# PersonaSafe example environment variables (no secrets)
# Copy to .env and fill in if needed.

# Hugging Face token (required for gated models like Gemma)
# Obtain from https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=

# Optional configuration for local paths (dashboard reads vectors/runs from these)
# Leave unset to use defaults (./vectors, ./runs)
# CACHE_DIR=./vectors
# RUNS_DIR=./runs

# Optional: API providers for future integrations (not used in v0.2 SFA)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
